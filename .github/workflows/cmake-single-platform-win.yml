# This starter workflow is for a CMake project running on a single platform. There is a different starter workflow if you need cross-platform coverage.
# See: https://github.com/actions/starter-workflows/blob/main/ci/cmake-multi-platform.yml
name: CMake pipeline for Windows build

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

env:
  # Customize the CMake build type here (Release, Debug, RelWithDebInfo, etc.)
  BUILD_TYPE: Release

jobs:
  build:
    # The CMake configure and build commands are platform agnostic and should work equally well on Windows or Mac.
    # You can convert this to a matrix build if you need cross-platform coverage.
    # See: https://docs.github.com/en/free-pro-team@latest/actions/learn-github-actions/managing-complex-workflows#using-a-build-matrix
    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v4
    - name: Setup MSYS2 (install needed packages)
      uses: msys2/setup-msys2@v2
      with:
        update: true
        install: >-
          mingw-w64-x86_64-gcc
          mingw-w64-x86_64-cmake
          mingw-w64-x86_64-ninja
          mingw-w64-x86_64-pkg-config
          mingw-w64-x86_64-zlib
          mingw-w64-x86_64-gcc-libs
          mingw-w64-x86_64-boost
          mingw-w64-x86_64-boost-libs	  
          mingw-w64-x86_64-qt5-base
          mingw-w64-x86_64-harfbuzz
          mingw-w64-x86_64-md4c
          mingw-w64-x86_64-double-conversion
          mingw-w64-x86_64-icu
          mingw-w64-x86_64-pcre2
          mingw-w64-x86_64-libpng
          mingw-w64-x86_64-zstd
          
    - name: Configure (MSYS2 / Ninja)
      shell: msys2 {0}
      run: cmake -S . -B build -G Ninja -DCMAKE_BUILD_TYPE=${{env.BUILD_TYPE}}

    - name: Build (MSYS2 / Ninja)
      shell: msys2 {0}
      run: cmake --build build --parallel

    - name: Test (MSYS2 / Ninja)
      shell: msys2 {0}
      run: |
        cd build
        ctest --output-on-failure
    - name: Collect all required MSYS2 .dll and .exe files from /mingw64/bin
      shell: msys2 {0}
      run: |
        mkdir -p build/src
        shopt -s nullglob
        files=(/mingw64/bin/*.dll /mingw64/bin/*.exe)
        if [ "${#files[@]}" -gt 0 ]; then
          cp -v "${files[@]}" build/src/
        else
          echo "No .dll or .exe files found in /mingw64/bin"
        fi
        ls -la build/src

    - name: Upload artifacts
      uses: actions/upload-artifact@v4
      with:
        name: cli-calculator-windows
        path: |
          ${{github.workspace}}/build/src/calculator*
          ${{github.workspace}}/build/src/divisors*
          ${{github.workspace}}/build/src/libgcc_s_seh-1.dll
          ${{github.workspace}}/build/src/zlib1.dll
          ${{github.workspace}}/build/src/libboost*.dll
          ${{github.workspace}}/build/src/libstdc++-6.dll
          ${{github.workspace}}/build/src/libwinpthread-1.dll
          ${{github.workspace}}/build/src/Qt5Concurrent.dll
          ${{github.workspace}}/build/src/Qt5Core.dll
          ${{github.workspace}}/build/src/Qt5DBus.dll
          ${{github.workspace}}/build/src/Qt5Gui.dll
          ${{github.workspace}}/build/src/Qt5Network.dll
          ${{github.workspace}}/build/src/Qt5OpenGL.dll
          ${{github.workspace}}/build/src/Qt5PrintSupport.dll
          ${{github.workspace}}/build/src/Qt5Sql.dll
          ${{github.workspace}}/build/src/Qt5Test.dll
          ${{github.workspace}}/build/src/Qt5Widgets.dll
          ${{github.workspace}}/build/src/Qt5Xml.dll
          ${{github.workspace}}/build/src/libharfbuzz-0.dll
          ${{github.workspace}}/build/src/libharfbuzz-gobject-0.dll
          ${{github.workspace}}/build/src/libharfbuzz-subset-0.dll
          ${{github.workspace}}/build/src/libmd4c.dll
          ${{github.workspace}}/build/src/libdouble-conversion.dll
          ${{github.workspace}}/build/src/libicuin78.dll
          ${{github.workspace}}/build/src/libicudt78.dll
          ${{github.workspace}}/build/src/libicuio78.dll
          ${{github.workspace}}/build/src/libicutest78.dll
          ${{github.workspace}}/build/src/libicutu78.dll
          ${{github.workspace}}/build/src/libicuuc78.dll
          ${{github.workspace}}/build/src/libpcre2-16-0.dll
          ${{github.workspace}}/build/src/libpcre2-32-0.dll
          ${{github.workspace}}/build/src/libpcre2-8-0.dll
          ${{github.workspace}}/build/src/libpcre2-posix-3.dll
          ${{github.workspace}}/build/src/libpng16-16.dll
          ${{github.workspace}}/build/src/libzstd.dll
        if-no-files-found: warn

